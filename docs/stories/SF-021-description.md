# SF-021 - Web Interface UX Specification

## Story

Create a UX specification for the Software Factory web interface before
implementing the UI, using the supplied dashboard screenshots as the design and
workflow reference.

## Plan

[x]: create this story document
[x]: create `docs/factory/ux/` structure
[x]: define screen map and navigation model
[x]: document all primary screens with data, actions and states
[x]: add static HTML wireframes for all primary screens
[x]: link the UX spec from `specs/specs.md`

## Work Log

- Created this story document first so the UX work is traceable before adding
  the documentation files.
- Added `docs/factory/ux/README.md` with goals, visual direction and the
  preferred server-rendered implementation approach.
- Added `screen-map.md` with routes, navigation, common layout and status
  language.
- Added per-screen specs for login, dashboard, stories, story detail, briefing,
  screenshots, agents, recent merged, downloads and settings.
- Added static HTML/CSS wireframes for all primary screens under
  `docs/factory/ux/wireframes/` using dummy data from the supplied screenshots.
- Linked the UX folder from `specs/specs.md` and `docs/factory/README.md`.
