<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- "App-updates"-scherm (UpdateInstaller.kt): zelf gedownloade APK's van andere beheerde
         projecten installeren, zelfde recept als robberts_assistent. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:label="Software Factory"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- FileProvider voor de update-installer: content://-URI naar de gedownloade APK in de
             cache-map, zodat de package-installer erbij kan zonder file://-URI's bloot te geven
             (verplicht sinds Android 7, StrictMode FileUriExposedException). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.updateprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Package-visibility (Android 11+): zonder deze <package>-declaraties gooit
             PackageManager.getPackageInfo() voor elk van deze apps een NameNotFoundException, ook
             als de app wél geïnstalleerd is — UpdateInstaller.installedVersionCode() zou dan altijd
             -1 ("niet geïnstalleerd") teruggeven. Namen moeten hier hardcoded staan (queries kunnen
             niet dynamisch/uit projects.yaml komen); bij een nieuwe apkPackages-entry in
             projects.yaml hoort een nieuwe regel hier. -->
        <package android:name="nl.vdzon.wind"/>
        <package android:name="nl.vdzon.groentetuin"/>
        <package android:name="nl.vdzon.notities"/>
        <package android:name="nl.vdzon.robberts_assistent"/>
    </queries>
</manifest>
