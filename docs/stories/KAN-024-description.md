# KAN-024 - Agent Tester Docker Image

Story:
Bouw de `agent-tester` Docker image met browser en cluster tooling.

Plan:
[ ]: extend agent-base
[ ]: install Playwright/Chromium
[ ]: install psql, kubectl and oc
[ ]: verify image build

Done / rationale:
- Nog niet geimplementeerd.
