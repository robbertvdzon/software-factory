# KAN-034 - Deployment Frontmatter Parser

Story:
Parse YAML-frontmatter uit `docs/factory/deployment.md`.

Plan:
[ ]: choose YAML parser
[ ]: parse branch and preview config
[ ]: validate required/optional fields
[ ]: add parser tests

Done / rationale:
- Nog niet geimplementeerd.
