# KAN-043 - Preview URL En Namespace Templating

Story:
Template preview URL en namespace op basis van PR nummer.

Plan:
[ ]: read template fields
[ ]: substitute `{pr_num}`
[ ]: pass `SF_PREVIEW_*` env-vars
[ ]: add tests

Done / rationale:
- Nog niet geimplementeerd.
