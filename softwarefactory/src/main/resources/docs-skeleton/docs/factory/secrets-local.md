# Local Secrets

Leg hier vast welke secrets of env-vars nodig zijn om de applicatie lokaal te
starten en te testen. Zet hier nooit echte secret-waardes in.

Voorbeelden:

- `APP_DATABASE_URL`: lokale database connection string.
- `APP_API_KEY`: ophalen uit 1Password item `<naam>`.
