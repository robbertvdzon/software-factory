# KAN-023 - Agent Base Docker Image

Story:
Bouw de `agent-base` Docker image.

Plan:
[ ]: add Dockerfile
[ ]: install JDK, Node, git, gh and jq
[ ]: include agent CLI jar
[ ]: verify local image build

Done / rationale:
- Nog niet geimplementeerd.
